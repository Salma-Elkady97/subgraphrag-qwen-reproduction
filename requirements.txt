# Versions pinned to match the deployment that produced the published numbers
# (NVIDIA A100-SXM4-80GB, CUDA 13.0). Adjust the +cu128 wheel suffix to match
# your local CUDA if needed.
torch==2.11.0+cu128
vllm==0.11.2
transformers>=4.43,<5
accelerate>=0.30
sentence-transformers>=2.7
datasets>=2.16
numpy>=1.26
pandas>=2.0
pyyaml>=6.0
tqdm>=4.66
networkx>=3.2
torch-geometric>=2.5
fuzzywuzzy>=0.18
python-Levenshtein>=0.21
pydantic>=2.0
scikit-learn>=1.3
wandb>=0.16
